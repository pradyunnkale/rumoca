{#- Placeholder template — rendering is handled by the rumoca-galec crate's
    render_rust() function via the special dispatch in target_manifest.rs.
    This file exists only to satisfy the build.rs manifest-template validator. -#}
